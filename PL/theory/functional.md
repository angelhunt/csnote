fp favours declarations over imperative statements. 

In practice, this means that one would prefer to iterate over the data structure using a map or reduce function rather than a for...of loop.

